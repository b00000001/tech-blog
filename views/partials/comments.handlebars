<div class="comments">
    {{#each this}}
    <section class="comment column is-size-one-quarter">
        <div class="meta notification is-dark">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>